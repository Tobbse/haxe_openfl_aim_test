{
    "version": "0.2.0",
    "configurations": [       
        {
            "name": "Launch Chrome",
            "type": "chrome",
            "preLaunchTask": "npm: build",
            "request": "launch",
            "file": "${workspaceFolder}/dist/index.html"
        }
    ]
}